# --- Build Output ---
target/
out/
build/

# --- IDE & System Files ---
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db

# --- Version Control ---
.git/
.gitignore

# --- Logs & generated Artifacts ---
*.log
logs/
*.pdf
*.png

# --- Maven Wrapper (Optional: Keep if you use mvnw inside Docker) ---
# !mvnw
# !.mvn

# --- Docker Files (Don't copy the Docker config into the image itself) ---
Dockerfile
docker-compose.yml
.dockerignore

# --- Local Archive Storage (Prevent copying thousands of old PDFs) ---
# Assuming you store them in a root folder like 'archived_results' or similar
Archived_*/
Merged_Transcript_*.pdf